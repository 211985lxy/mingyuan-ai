services:
  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
      target: web-runner
    container_name: mingyuan-web
    restart: unless-stopped
    env_file:
      - ${MINGYUAN_WEB_ENV_FILE:-./apps/web/.env.production}
    environment:
      DATABASE_URL: mysql://mingyuan:${MYSQL_PASSWORD}@mysql:3306/mingyuan
      REDIS_URL: redis://redis:6379
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    ports:
      - "127.0.0.1:3000:3000"
    networks:
      - mingyuan-prod

  mysql:
    image: mysql:8.0
    container_name: mingyuan-prod-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: mingyuan
      MYSQL_USER: mingyuan
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    volumes:
      - mysql_data:/var/lib/mysql
    ports:
      - "127.0.0.1:3306:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-p${MYSQL_ROOT_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - mingyuan-prod

  redis:
    image: redis:7-alpine
    container_name: mingyuan-prod-redis
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data
    ports:
      - "127.0.0.1:6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - mingyuan-prod

  video-extractor:
    profiles: ["video-extractor"]
    build:
      context: services/video-extractor
    container_name: mingyuan-video-extractor
    restart: unless-stopped
    environment:
      VIDEO_EXTRACTOR_API_KEY: ${VIDEO_EXTRACT_FALLBACK_API_KEY}
      WHISPER_MODEL: ${WHISPER_MODEL:-small}
      WHISPER_DEVICE: ${WHISPER_DEVICE:-cpu}
      WHISPER_COMPUTE_TYPE: ${WHISPER_COMPUTE_TYPE:-int8}
      F2_DOUYIN_ENABLED: ${F2_DOUYIN_ENABLED:-true}
      DOUYIN_COOKIE: ${DOUYIN_COOKIE:-}
    volumes:
      - video_extractor_data:/data
    networks:
      - mingyuan-prod

volumes:
  mysql_data:
  redis_data:
  video_extractor_data:

networks:
  mingyuan-prod:
    driver: bridge
