# 客户演示前修复包 — 收尾结果

- 日期：2026-08-02
- 范围：领秀/宇米演示黄金路径，本地修复与真实浏览器验收
- 交付：本地 `main` fast-forward；未 push、未部署
- 总体状态：**代码与本地演示环境绿灯；线上仍是旧版本，部署前保持黄灯**

## 已完成修复

| 项目 | 结果 |
|---|---|
| 专家切换丢失 `projectId` | 已修复，侧栏切换保留项目范围 |
| 流量/获客/故事目的累积 | 已修复，三目的互斥替换 |
| 正文泄漏 `===END FORMAT===` | 已修复，生成与历史读取同源清洗 |
| 作品编辑交接刷新丢失 | 已修复，交接 URL 保留 `generationId` |
| 知识库编辑/归档按钮无名称 | 已补 `aria-label` 并通过组件测试 |
| 单条口播超时 | 新增单稿快速路由：GPT-4o、2500 tokens、22 秒 provider 超时、28 秒总预算、单 provider、零自动重试 |
| 快速链路额外模型评分拖慢 | 已关闭；模型评分只作提示，确定性门禁负责拦截 |
| 未批准数字/客户案例扩写 | 已加闭集事实门禁：模型只生成开头，批准事实与 CTA 从用户原文确定性回填；额外数字、衍生比例和客户细节被拦截/清理 |
| 技能菜单请求 500 | 找到缺失的 `AimCustomSkill` 建表迁移并补齐；本地演示库应用后 API 返回 200，技能菜单正常打开 |
| 旧迁移 MySQL 语法错误 | 将 `ADD COLUMN IF NOT EXISTS` 改为仓库通用的 `information_schema + PREPARE` 幂等写法 |

## 真实浏览器验收

使用已登录的脱敏演示账号与项目 `cms68a56w0001bf9knimj7wyy`，实际操作而非仅跑组件测试：

- 知识库：搜索、分类、手工新增弹窗、粘贴记忆弹窗、编辑弹窗、归档确认、取消操作均正常；未删除、未归档真实数据。
- 内容目的：连续点击“我要搞流量 / 我要获客 / 我要讲故事”，每次只保留当前锚点，无累积。
- 技能菜单：补迁移前 `/api/aim/skills` 为 500；补迁移后为 200，三个内置目的均可见。
- 黄金路径：内容创作 → 意图确认 → 生成 → 成稿查看，连续三次成功。
- 三次前端完整耗时：**4.83s / 3.82s / 3.82s**，全部低于 30 秒。
- 三次成稿均包含 `60天 / 40条 / 127条 / 800元 / 210元`，明确目标客户、痛点与评论领取 CTA。
- 三次成稿均未出现 `赋能 / 抓手 / 生态 / ===END FORMAT===`，没有新增客户数字、衍生比例或其他客户案例信息。
- 最新生成记录：`agentId=content_producer`、`model=gpt-4o`、`status=completed`。

证据位于 Git 忽略目录：

- `apps/web/output/playwright/demo-closeout/final-content-producer.png`
- `apps/web/output/playwright/demo-closeout/skills-menu-after-migration.png`
- `apps/web/output/playwright/demo-closeout/cli-artifacts/`

## 自动化门禁

| 门禁 | 结果 |
|---|---|
| 单元回归 | 12 文件、158 例通过 |
| 知识库组件回归 | 3 文件、8 例通过 |
| 快速路由最终回归 | 3 文件、52 例通过 |
| TypeScript | 通过 |
| ESLint 改动文件 | 0 error；2 个既有文件长度 warning |
| `arch:size` | 通过，`functions>80:184 baseline=184` |
| API contracts | 通过，240 routes |
| env contract | 通过，221 variables |
| Next.js 生产构建 | 通过，生成 `.next/BUILD_ID` |

`schema:migration-integrity` 仍报“baseline 不是迁移历史有序前缀”，与本次新增迁移无关，是仓库既有 baseline 清单问题；本次新增迁移已在本地演示库直接执行并通过 API 验证。

## Go / No-Go

- **本地演示：Go。** 黄金路径、技能菜单与知识库按钮均已验证。
- **当前线上：暂不宣称已修复。** `https://mingyuan-ai.cn` 仍运行旧 release；本次按约定没有 push 或部署。
- 若演示前仍不部署：使用本报告中的已验证截图和最新成稿作为备用方案。
- 若获批部署：先执行数据库迁移，再部署本地 `main`，随后只做健康、版本、登录、知识读取和已有成稿冒烟；任一 P0 则使用备用方案。
