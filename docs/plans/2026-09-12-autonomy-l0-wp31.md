# WP-3.1 L0 自主放行

> 状态：代码完成（默认关）· 日期：2026-09-12 · 分支：`feat/wp11-outcome-autofetch`（尚未拆 PR）
> 上游：批次 3 / WP-3.1
> 红线：对外发送、报价、承诺、知识库正本永不走 L0；开关默认关。

## 1. 改了什么

- 首个动作：`inspiration_duplicate_prescreen`。重复且能解析作品链接 → 可 autoPass；否则仍 L2。
- 开着时记审批台账（decision=approve，reason 带 `L0 auto_pass`），可审计。
- 30 天内出现申诉级误判 → `shouldDemoteAutonomyL0` 应降回 L2。降级自动执行还没接到告警通道。

## 2. 不做

- 不在生产打开开关。
- 不改原灵感记录的处理流水（重复早退本来就不新建）。
- 不宣称「灰度 30 天零事故」。

## 3. 回滚

`AIM_AUTONOMY_L0_ENABLED` 保持 false。打开后出问题立刻改回 false。
