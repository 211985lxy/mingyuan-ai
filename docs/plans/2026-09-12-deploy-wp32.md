# WP-3.2 部署收口（设计记录，不动生产）

> 状态：设计完成 · 代码层未改生产流水线 · 日期：2026-09-12
> 上游：批次 3 / WP-3.2
> 红线：不改 `k8s/`、`docker-compose.prod.yml`、deploy workflow 的 SSH/K8s 作业；不在本地终端对生产执行部署。

## 建议（方案 A）

现有事实：

- 真正上线走 `scripts/deploy-ecs-standalone.sh`（本机 build → rsync → systemctl）。
- `.github/workflows/deploy.yml` 已有 quality gate，且 `concurrency.group: production-deploy`、`cancel-in-progress: false`。ACR→K8s 从未跑通，workflow 目前是手动触发。

收口做法（下次运维批准后再改 yaml，本次不加）：

1. 在 `deploy.yml` 的 quality job 之后加 `deploy-ecs` job，SSH 到现有 ECS 跑同一脚本，不新建第二套发布路径。
2. 互斥锁从脚本层的 flock 上移复用现有 `production-deploy` concurrency；同时脚本内 flock 保留作防御。
3. 预发：独立 MySQL/Redis + 独立 env，同一产物双投递；`aim-eval-daily` 先在预发跑。

## 明确不做

- 本次不配 GitHub Secrets，不恢复 `on.push.main` 自动部署。
- 不修 ACR→K8s（方案 B 排在 A 之后）。
- 不宣称「连续 3 次发布全程 CI 内完成」。
