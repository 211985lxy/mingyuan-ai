# WP-3.3 工具环 + 复盘归因 loop

> 状态：代码完成（shadow）· 日期：2026-09-12 · 分支：`feat/wp11-outcome-autofetch`（尚未拆 PR）
> 上游：批次 3 / WP-3.3
> 红线：写工具没有 HITL 批准一律拒绝；复盘初稿不外发；正式模式未开时恒 shadow。

## 1. 改了什么

- 注册 3 个幂等写工具：`feishu_draft_write`、`mark_inspiration_processed`（可进工具环但须 HITL）、`review_attribution_loop`（禁止进自由工具环）。
- HITL 门闩：没 `approve` 就返回拒绝；有批准也只返回 shadow，不真正写飞书/改库。
- 复盘归因 loop 照 sales-diagnosis 8 步 id，消费指标层出周复盘初稿。

## 2. 不做

- 不把写工具加入默认主动作清单（模型不会随便调到）。
- 不新增飞书工作流类型，不注册第四个 BusinessLoopId（那要改经营事项枚举）。
- 不切正式模式。
