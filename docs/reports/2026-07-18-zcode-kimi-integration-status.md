# Z Code / Kimi 整合状态

更新时间：2026-07-18

## 已实现并纳入候选分支

- 候选基线治理与分阶段整合文档：已提交；候选分支保持干净。
- 作品版本数据层：`AimContentVersion` 模型、迁移、列表/创建/恢复 API 已提交；恢复只追加新版本，并用事务锁避免版本号竞态。
- 统一创作台路由契约：社媒速产、深度长文、自由交付三个模块键已接入“内容创作官”入口；非法模块在请求契约层拒绝，生产模型顺序未改变，旧深链和历史 ID 保留。
- 作品编辑官（本轮范围）：复用原深度长文深链作为独立“作品编辑官”入口，已接入版本时间线/恢复、公众号主题预览与双格式复制、小红书标题/正文/标签检查和变体提示；视频剪辑暂缓，不从产品流程中误删，只延后实现。
- Token usage 与成本观测：非流式 provider usage、trace 成本字段、价格估算与测试已提交；价格仅为观测估算，不作为计费依据。流式 provider 未返回 usage 时不虚构成本。
- 客户端/服务端导入边界修复：已验证，候选分支当前保持客户端安全形态。
- Kimi PR #17 的客户端/服务端导入边界修复：已验证，候选分支当前保持客户端安全形态。
- Z Code 的思考过程、Token 优化和 PR 审查计划：已转化为本次整合顺序与验收门槛。

## 仅有方案，尚未实现

- Z Code 的抖音群 → 飞书 → AIM → Base 全链路：缺少中转服务、Base 标识和生产部署位置。
- 视频号 → 公众号 → AIM 链路：域名已识别，但 Provider 仍明确失败；缺少服务商契约、Key、响应样本和真实失败语义，未启用。
- Z Code 的 Handler 拆分与 Prisma 多文件拆分：尚未进入本轮候选整合，需独立工作包和行为回归门禁。

## 处理原则

- 计划文件不视为代码完成；实现状态以候选分支代码、测试、构建和真实联调为准。
- 未完成外部依赖的能力保留为明确失败的配置边界，不开放为“已支持”。
- 每个业务包独立提交，发生冲突时保留候选分支的 90 天升级、Harness、权限和数据飞轮语义。
- 当前候选业务包提交：`00c90de`（版本 API）、`6da5afc`（统一创作台入口与模块路由）、`4743de2`（版本时间线/恢复）、`dd911a3`（公众号预览与复制）、`f0656a7`（小红书检查与变体入口）、`aae343d`（编辑安全基础）、`3d97662`（成本观测）、`731d234`（视频号失败边界）。
