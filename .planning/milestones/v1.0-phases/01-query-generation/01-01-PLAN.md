---
phase: 01-query-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/api/packaging-material-suggestions/route.ts
autonomous: true
requirements: [QGEN-01, QGEN-02, QGEN-03, QGEN-04]

must_haves:
  truths:
    - "getFallbackQuery for a Chinese HVAC industry input returns English visual archetype terms, not Chinese strings or 'small business'"
    - "The LLM system prompt contains an industry visual vocabulary table mapping Chinese categories to English stock photo terms"
    - "The LLM system prompt contains role-specific visual semantics guidance for product_detail, store_environment, and process"
    - "The LLM system prompt contains few-shot good-vs-bad query examples across diverse industries"
    - "The LLM output schema includes a rationale field and maxTokens is raised to 1200"
  artifacts:
    - path: "apps/web/src/app/api/packaging-material-suggestions/route.ts"
      provides: "Rewritten getFallbackQuery with resolveVisualArchetype + rewritten buildSearchPlan system prompt"
      contains: "resolveVisualArchetype"
  key_links:
    - from: "getFallbackQuery"
      to: "resolveVisualArchetype"
      via: "function call to map Chinese industry to English archetype"
      pattern: "resolveVisualArchetype\\(input\\.industry"
    - from: "buildSearchPlan systemPrompt"
      to: "LLMClient.shared().complete()"
      via: "system message content"
      pattern: "角色语义规范"
---

<objective>
Rewrite the LLM system prompt and deterministic fallback in the packaging-material-suggestions route to produce industry-specific, role-aware English search queries instead of vague generic keywords.

Purpose: Fix the root cause of irrelevant stock media results (e.g., HVAC business getting birds and lakes) by anchoring query generation to concrete industry visual vocabulary and role-specific semantics.
Output: Modified `route.ts` with rewritten `getFallbackQuery()`, new `resolveVisualArchetype()` function, and completely rewritten `buildSearchPlan()` system prompt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-query-generation/01-RESEARCH.md

@apps/web/src/app/api/packaging-material-suggestions/route.ts
@apps/web/src/lib/packaging-materials.ts
@apps/web/src/lib/ip-profile.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From apps/web/src/lib/packaging-materials.ts:
```typescript
export const SAFE_AI_MATERIAL_ROLES = ["product_detail", "store_environment", "process"] as const;
export type SafeAiMaterialRole = (typeof SAFE_AI_MATERIAL_ROLES)[number];
```

From apps/web/src/lib/ip-profile.ts:
```typescript
export function buildIpProfilePromptSnapshot(input: Partial<IpProfileInput>): string;
// Returns Chinese-language profile snapshot with fields: industry, primaryOffer, targetAudience, etc.
```

From route.ts (current signatures to modify):
```typescript
type SafeRole = (typeof SAFE_AI_MATERIAL_ROLES)[number];

function getFallbackQuery(role: SafeRole, input: {
  industry?: string | null;
  primaryOffer?: string | null;
  targetAudience?: string | null;
}): string;

// buildSearchPlan systemPrompt at line 170, maxTokens at line 216
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite getFallbackQuery with resolveVisualArchetype mapping</name>
  <files>apps/web/src/app/api/packaging-material-suggestions/route.ts</files>
  <read_first>
    - apps/web/src/app/api/packaging-material-suggestions/route.ts (lines 98-117 — current getFallbackQuery)
    - apps/web/src/lib/packaging-materials.ts (SAFE_AI_MATERIAL_ROLES definition)
    - .planning/phases/01-query-generation/01-RESEARCH.md (Pattern 5: getFallbackQuery Rewrite)
  </read_first>
  <action>
Replace the existing `getFallbackQuery` function (lines 98-117) and add a new `resolveVisualArchetype` function immediately above it. The changes are:

1. **Add `resolveVisualArchetype` function** (place it between the `distributeCounts` function ending at line 96 and the current `getFallbackQuery` at line 98):

```typescript
function resolveVisualArchetype(
  industry?: string | null,
  primaryOffer?: string | null,
): string {
  const combined = `${industry ?? ""} ${primaryOffer ?? ""}`.toLowerCase();
  if (/空调|暖通|hvac/i.test(combined)) return "HVAC technician air conditioning";
  if (/烘焙|面包|蛋糕|甜品/i.test(combined)) return "baker pastry bakery";
  if (/月子|产后|母婴/i.test(combined)) return "postnatal care newborn";
  if (/家具|衣柜|木工|定制/i.test(combined)) return "carpenter woodwork furniture";
  if (/美容|整形|医美|皮肤/i.test(combined)) return "medical aesthetic beauty treatment";
  if (/餐饮|火锅|餐厅|厨师/i.test(combined)) return "restaurant kitchen food";
  if (/健身|瑜伽|体育|运动/i.test(combined)) return "fitness gym workout";
  if (/汽车|车|洗车|修车/i.test(combined)) return "auto mechanic car service";
  if (/教育|培训|辅导|课程/i.test(combined)) return "education classroom training";
  if (/零售|服装|时装|商店/i.test(combined)) return "retail store merchandise";
  if (/房产|地产|房屋|置业/i.test(combined)) return "real estate property home";
  if (/法律|律师|法务/i.test(combined)) return "lawyer legal office consultation";
  if (/牙科|口腔|牙医/i.test(combined)) return "dentist dental clinic";
  if (/美发|理发|发型/i.test(combined)) return "hair salon stylist";
  if (/摄影|拍照|写真/i.test(combined)) return "photography studio portrait";
  if (/宠物|猫|狗/i.test(combined)) return "pet care veterinary";
  if (/早教|幼儿|托育/i.test(combined)) return "early childhood daycare";
  if (/保洁|家政|清洁/i.test(combined)) return "home cleaning housekeeping";
  if (/物流|快递|配送/i.test(combined)) return "logistics delivery warehouse";
  if (/花店|花艺|鲜花/i.test(combined)) return "florist flower arrangement";
  // Generic last resort — better than "small business"
  console.warn("[packaging-material-suggestions] resolveVisualArchetype: no match for", combined.trim());
  return "professional service business";
}
```

2. **Rewrite `getFallbackQuery`** to call `resolveVisualArchetype` instead of using raw Chinese strings:

```typescript
function getFallbackQuery(role: SafeRole, input: {
  industry?: string | null;
  primaryOffer?: string | null;
  targetAudience?: string | null;
}): string {
  const englishArchetype = resolveVisualArchetype(input.industry, input.primaryOffer);
  switch (role) {
    case "product_detail":
      return `${englishArchetype} detail close-up`;
    case "store_environment":
      return `${englishArchetype} workplace interior`;
    case "process":
      return `${englishArchetype} professional work`;
  }
}
```

**Key changes from current code:**
- Removes `const subject = offer || industry || "small business"` which passes Chinese text directly into English queries
- Removes `const context = audience ? ...` which appended Chinese audience text
- Replaces with English-only visual archetype strings via regex matching on ~20 Chinese industry categories
- Generic fallback changes from "small business" to "professional service business"
- Adds console.warn when falling through to generic fallback to make coverage gaps visible in production logs

**Do NOT change:** The `getFallbackQuery` function signature, the `SafeRole` type, the return type (still `string`), or any code outside this function and the new `resolveVisualArchetype` function.
  </action>
  <verify>
    <automated>cd /Users/ethan/Workspace/z/clipflow/apps/web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - route.ts contains `function resolveVisualArchetype(`
    - route.ts contains `if (/空调|暖通|hvac/i.test(combined)) return "HVAC technician air conditioning"`
    - route.ts contains `return "professional service business"` as the generic fallback
    - route.ts contains `console.warn("[packaging-material-suggestions] resolveVisualArchetype:`
    - route.ts contains `const englishArchetype = resolveVisualArchetype(input.industry, input.primaryOffer)`
    - route.ts does NOT contain `const subject = offer || industry || "small business"`
    - route.ts does NOT contain `const context = audience ?`
    - TypeScript compiles without errors (`npx tsc --noEmit` exits 0)
  </acceptance_criteria>
  <done>
    getFallbackQuery returns English visual archetype strings for all 20 mapped Chinese industry categories, and "professional service business" for unmapped industries. No Chinese text can leak into query strings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite buildSearchPlan system prompt with industry vocabulary, role semantics, few-shot examples, and rationale field</name>
  <files>apps/web/src/app/api/packaging-material-suggestions/route.ts</files>
  <read_first>
    - apps/web/src/app/api/packaging-material-suggestions/route.ts (lines 170-218 — current systemPrompt + maxTokens)
    - .planning/phases/01-query-generation/01-RESEARCH.md (Patterns 1-4: rationale field, vocabulary table, role semantics, few-shot examples)
  </read_first>
  <action>
Replace the `systemPrompt` string (lines 170-189) and update `maxTokens` (line 216) in `buildSearchPlan()`. Also update the parsed type on line 226 to include the `rationale` field.

1. **Replace the entire `systemPrompt` constant** (currently lines 170-189) with this new prompt:

```typescript
  const systemPrompt = `你是包装层素材规划助手。目标是为一条营销短视频补充通用支持型画面素材，query 必须精准匹配用户的具体行业和业务内容。

只能使用这些角色：
- product_detail
- store_environment
- process

禁止输出这些角色：
- customer_case
- qualification
- before_after

【行业视觉词汇表】
以下是常见行业对应的英文图库搜索关键词，query 必须优先使用这些词汇：
- 空调维修/暖通: HVAC technician, outdoor unit, air conditioning equipment, ductwork, compressor
- 烘焙/面包店: baker, bread dough, oven, pastry, flour, croissant, bakery
- 医疗美容/整形: medical aesthetic, beauty treatment, facial treatment, clinic interior, skincare
- 定制家具/衣柜: carpenter, woodwork, furniture installation, cabinet, wood grain
- 月子中心/产后护理: newborn care, nurse, postnatal, mother infant, nursery room
- 餐饮/火锅/餐厅: restaurant kitchen, food plating, chef, dining table, cooking
- 健身/瑜伽: fitness trainer, yoga pose, gym equipment, workout, exercise
- 教育/培训: classroom, teacher, student, learning, study, education
- 汽车美容/维修: car detailing, mechanic, auto repair, garage, vehicle
- 零售/服装: retail store, clothing rack, fashion display, shopping, merchandise
- 房产/地产: real estate, property, home interior, house exterior, architecture
- 法律/律师: lawyer, legal office, consultation, courtroom, contract
- 牙科/口腔: dentist, dental clinic, teeth, oral care, dental equipment
- 美发/理发: hair salon, stylist, hairdressing, beauty chair, scissors
- 摄影/写真: photography studio, portrait, camera, lighting setup, photo shoot
- 宠物/猫狗: pet care, veterinary, dog grooming, cat, animal clinic
- 早教/幼儿: early childhood, daycare, children playing, learning toys, nursery
- 保洁/家政: home cleaning, housekeeping, cleaning supplies, mop, tidy room
- 物流/快递: logistics, delivery, warehouse, package, shipping
- 花店/花艺: florist, flower arrangement, bouquet, flower shop, floral design
如果用户的行业不在上表，请根据行业本质推断最接近的英文视觉描述词。

角色语义规范（必须遵守）：
- product_detail: 聚焦产品/服务工具的特写镜头。query 应描述实物物体、设备细节、材质纹理。
  示例: "air conditioning unit close-up", "woodworking chisel detail", "dental equipment close-up"
- store_environment: 聚焦经营场所的空间感。query 应描述室内或工作场地的整体环境。
  示例: "bakery shop interior", "auto repair garage workshop", "dental clinic interior"
- process: 聚焦人物正在执行专业操作的动态画面。query 应描述具体工作过程。
  示例: "HVAC technician installing outdoor unit", "baker kneading bread dough", "dentist examining patient"

【正确示例 vs 错误示例】

错误：行业=空调维修，role=product_detail → query="repair work detail" （太泛，Pexels 返回不相关结果）
正确：行业=空调维修，role=product_detail → query="air conditioning outdoor compressor unit close-up"

错误：行业=烘焙，role=process → query="food making process" （太泛）
正确：行业=烘焙，role=process → query="baker kneading bread dough hands"

错误：行业=月子中心，role=store_environment → query="health center interior" （太泛）
正确：行业=月子中心，role=store_environment → query="postnatal care room newborn nursery warm"

输出要求：
1. 仅返回 JSON
2. JSON 结构必须是 {"queries":[{"role":"product_detail","mediaType":"image","rationale":"推理过程","query":"english keywords","count":3}]}
3. rationale 字段：用中文简述为何选择这个 query（不超过30字），写在 query 之前
4. query 字段只允许英文单词，不得包含任何中文字符
5. query 必须包含行业特定的视觉主体词，禁止使用 "small business"、"work process"、"service detail" 等泛化词汇
6. mediaType 只能是 image 或 video，process 优先 video，其它角色默认 image
7. 每个 count 必须是正整数
8. 所有 count 总和尽量接近 ${input.maxCount}
9. 不要输出人脸特写，不要输出品牌名，不要输出假资质或假案例`;
```

2. **Update `maxTokens`** from 900 to 1200 (line 216):

```typescript
      maxTokens: 1200,
```

3. **Update the parsed type** (line 226) to include `rationale`:

```typescript
    const parsed = JSON.parse(raw) as {
      queries?: Array<{
        role?: string;
        mediaType?: string;
        query?: string;
        rationale?: string;
        count?: number;
      }>;
    };
```

The `rationale` field is parsed from the JSON but never stored or returned. The existing normalization code on lines 230-251 already only maps `role`, `mediaType`, `query`, and `count` into `SearchPlanEntry` — the `rationale` is automatically discarded.

**Do NOT change:** The `userPrompt`, the `SearchPlanEntry` interface, the normalization/rescaling logic, the `try/catch` fallback behavior, or any code outside the `systemPrompt` string, `maxTokens` value, and the parsed type.
  </action>
  <verify>
    <automated>cd /Users/ethan/Workspace/z/clipflow/apps/web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - route.ts systemPrompt contains "【行业视觉词汇表】"
    - route.ts systemPrompt contains "角色语义规范（必须遵守）"
    - route.ts systemPrompt contains "【正确示例 vs 错误示例】"
    - route.ts systemPrompt contains "rationale" in the JSON structure example
    - route.ts systemPrompt contains "query 字段只允许英文单词，不得包含任何中文字符"
    - route.ts systemPrompt contains "空调维修/暖通: HVAC technician"
    - route.ts contains `maxTokens: 1200`
    - route.ts does NOT contain `maxTokens: 900`
    - route.ts parsed type contains `rationale?: string`
    - route.ts systemPrompt does NOT contain "通用支持型画面素材" without the industry-specificity clause (the old generic instruction)
    - TypeScript compiles without errors (`npx tsc --noEmit` exits 0)
  </acceptance_criteria>
  <done>
    The LLM system prompt includes: (1) industry visual vocabulary table with 20 Chinese categories mapped to English stock photo terms, (2) role-specific visual semantics for product_detail/store_environment/process, (3) three good-vs-bad few-shot examples, (4) rationale field requirement in JSON output schema. maxTokens raised to 1200. Existing normalization code automatically discards the rationale field.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd apps/web && npx tsc --noEmit` — TypeScript compiles cleanly
2. `grep -c "resolveVisualArchetype" apps/web/src/app/api/packaging-material-suggestions/route.ts` returns at least 2 (definition + call)
3. `grep "small business" apps/web/src/app/api/packaging-material-suggestions/route.ts` returns zero matches
4. `grep "maxTokens: 1200" apps/web/src/app/api/packaging-material-suggestions/route.ts` returns one match
5. `grep "行业视觉词汇表" apps/web/src/app/api/packaging-material-suggestions/route.ts` returns one match
</verification>

<success_criteria>
- getFallbackQuery returns English-only strings for 20 mapped Chinese industries
- System prompt contains industry vocabulary table, role semantics, few-shot examples, and rationale field
- maxTokens increased from 900 to 1200
- No Chinese text can leak into Pexels query strings via the deterministic fallback path
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-query-generation/01-01-SUMMARY.md`
</output>
