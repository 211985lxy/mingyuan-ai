---
phase: 05-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/messages/zh.json
  - apps/web/messages/en.json
  - apps/web/src/components/marketing/hero-section.tsx
  - apps/web/src/components/marketing/pain-points-section.tsx
  - apps/web/src/components/marketing/how-it-works-section.tsx
  - apps/web/src/components/marketing/features-section.tsx
  - apps/web/src/components/marketing/differentiators-section.tsx
  - apps/web/src/components/marketing/social-proof-section.tsx
  - apps/web/src/components/marketing/cta-section.tsx
  - apps/web/src/app/(marketing)/page.tsx
autonomous: true
requirements:
  - SECT-01
  - SECT-02
  - SECT-03
  - SECT-04
  - SECT-05
  - SECT-06
  - SECT-07
  - SECT-08

must_haves:
  truths:
    - "User sees a hero section with an outcome-focused headline, subtitle, and two CTA buttons above the fold on 375px"
    - "User sees pain points, how-it-works, features, differentiators, social proof, and final CTA sections in sequence"
    - "All copy is driven from messages/zh.json and messages/en.json — zero hardcoded strings in components"
    - "Switching language updates all section copy without a full page reload"
    - "npm run build completes without errors"
  artifacts:
    - path: "apps/web/src/components/marketing/hero-section.tsx"
      provides: "Hero section with headline, subtitle, dual CTA"
      contains: "HeroSection"
    - path: "apps/web/src/components/marketing/pain-points-section.tsx"
      provides: "Pain points grid"
      contains: "PainPointsSection"
    - path: "apps/web/src/components/marketing/how-it-works-section.tsx"
      provides: "3-step how it works"
      contains: "HowItWorksSection"
    - path: "apps/web/src/components/marketing/features-section.tsx"
      provides: "3 feature cards"
      contains: "FeaturesSection"
    - path: "apps/web/src/components/marketing/differentiators-section.tsx"
      provides: "6 differentiator items"
      contains: "DifferentiatorsSection"
    - path: "apps/web/src/components/marketing/social-proof-section.tsx"
      provides: "3 social proof stats"
      contains: "SocialProofSection"
    - path: "apps/web/src/components/marketing/cta-section.tsx"
      provides: "Final CTA section"
      contains: "CTASection"
    - path: "apps/web/src/app/(marketing)/page.tsx"
      provides: "Landing page assembling all sections"
      contains: "HeroSection"
---

<objective>
Build all 7 landing page content sections with real bilingual copy, completing the full marketing page that converts visitors in both Chinese and English.

Purpose: Phase 4 built the infrastructure shell (navbar, footer, i18n). This phase fills it with content — seven section components driven entirely by translation files. After this phase, a visitor can read the full landing page in either language.

Output: Complete landing page at / with all sections assembled, real copy in both languages, zero hardcoded strings.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Fill translation files with real bilingual copy</name>
  <files>
    apps/web/messages/zh.json
    apps/web/messages/en.json
  </files>
  <action>
Update both translation files with real marketing copy. Zero empty strings allowed — every key must have its final value.

**apps/web/messages/zh.json:**
```json
{
  "Navbar": {
    "logo": "ClipFlow",
    "cta": "免费开始"
  },
  "Hero": {
    "title": "60 秒，一条专业营销短视频",
    "subtitle": "输入你的文案，ClipFlow 自动完成脚本创作、数字人配音、字幕包装——让每个小企业主都拥有专属视频团队",
    "cta": "免费开始创作",
    "ctaSecondary": "查看演示"
  },
  "PainPoints": {
    "heading": "你是否也有这些烦恼？",
    "pain1Title": "拍摄成本高",
    "pain1Desc": "专业摄影团队动辄数千元，普通老板根本承受不起",
    "pain2Title": "剪辑太耗时",
    "pain2Desc": "一条视频从拍摄到发布需要数天时间，严重拖慢内容节奏",
    "pain3Title": "不会写文案",
    "pain3Desc": "不知道如何写出吸引人的营销文案，内容平淡无法转化",
    "pain4Title": "更新频率低",
    "pain4Desc": "每天更新视频几乎不可能，账号缺乏持续活跃度"
  },
  "HowItWorks": {
    "heading": "三步生成专业营销短视频",
    "step1Title": "输入你的文案",
    "step1Desc": "填写产品卖点或直接描述你的业务，AI 自动优化成专业营销脚本",
    "step2Title": "选择数字人形象",
    "step2Desc": "从数十位专业数字人中选择，或克隆你自己的形象，保持 IP 一致性",
    "step3Title": "一键生成视频",
    "step3Desc": "AI 自动完成配音、字幕、包装——60 秒内获得发布就绪的营销短视频"
  },
  "Features": {
    "heading": "专为小企业主设计的功能",
    "feature1Title": "AI 脚本生成",
    "feature1Desc": "基于你的 IP 档案和行业特点，自动生成符合营销逻辑的短视频脚本",
    "feature2Title": "数字人 & 语音克隆",
    "feature2Desc": "专业数字人库加个人声音克隆，打造独一无二的品牌 IP 形象",
    "feature3Title": "追热点创作",
    "feature3Desc": "实时抖音热榜接入，一键将热点话题转化为你的营销内容"
  },
  "Differentiators": {
    "heading": "为什么选择 ClipFlow？",
    "diff1Title": "无需拍摄技能",
    "diff1Desc": "全程 AI 驱动，小白也能制作专业级营销视频",
    "diff2Title": "成本降低 90%",
    "diff2Desc": "对比专业团队外包，每条视频制作成本不到 10 元",
    "diff3Title": "发布速度 10 倍",
    "diff3Desc": "从灵感到发布全程自动化，1 小时完成传统团队 1 周的工作量",
    "diff4Title": "IP 一致性保障",
    "diff4Desc": "IP 档案驱动每一个脚本，品牌形象始终如一",
    "diff5Title": "多平台适配",
    "diff5Desc": "支持抖音、视频号、小红书等主流平台格式，一键多发",
    "diff6Title": "实时热点追踪",
    "diff6Desc": "内置抖音热榜，随时把握流量热点，让你的内容永远在风口"
  },
  "SocialProof": {
    "heading": "数千位小企业主的信任之选",
    "stat1Value": "10,000+",
    "stat1Label": "已生成视频",
    "stat2Value": "3,000+",
    "stat2Label": "企业主用户",
    "stat3Value": "60秒",
    "stat3Label": "平均生成时间"
  },
  "CTA": {
    "heading": "立即开启你的 IP 之旅",
    "subtext": "免费注册，每天可生成 2 条视频，无需信用卡",
    "button": "免费开始"
  },
  "Footer": {
    "copyright": "ClipFlow 2026",
    "tagline": "AI 驱动的营销短视频全自动流水线"
  }
}
```

**apps/web/messages/en.json:**
```json
{
  "Navbar": {
    "logo": "ClipFlow",
    "cta": "Start Free"
  },
  "Hero": {
    "title": "A Professional Marketing Video in 60 Seconds",
    "subtitle": "Enter your copy, ClipFlow handles script writing, AI avatar voiceover, and subtitle packaging — every small business deserves its own video team",
    "cta": "Start Creating Free",
    "ctaSecondary": "See a Demo"
  },
  "PainPoints": {
    "heading": "Do Any of These Sound Familiar?",
    "pain1Title": "High Production Costs",
    "pain1Desc": "Professional video teams charge thousands per shoot — way beyond a small business budget",
    "pain2Title": "Editing Takes Days",
    "pain2Desc": "From filming to publishing can take a week, killing the pace of your content calendar",
    "pain3Title": "Copywriting Is Hard",
    "pain3Desc": "Not knowing how to write converting copy means your content falls flat and doesn't drive sales",
    "pain4Title": "Inconsistent Output",
    "pain4Desc": "Posting every day is nearly impossible, leaving your accounts inactive and losing the algorithm"
  },
  "HowItWorks": {
    "heading": "Three Steps to a Professional Marketing Video",
    "step1Title": "Enter Your Copy",
    "step1Desc": "Describe your product benefits or business — AI automatically refines it into a professional marketing script",
    "step2Title": "Choose an AI Avatar",
    "step2Desc": "Pick from dozens of professional digital humans, or clone your own likeness for consistent personal branding",
    "step3Title": "Generate in One Click",
    "step3Desc": "AI handles voiceover, subtitles, and packaging — your publish-ready video is ready in 60 seconds"
  },
  "Features": {
    "heading": "Features Built for Small Business Owners",
    "feature1Title": "AI Script Generation",
    "feature1Desc": "Auto-generates marketing scripts based on your IP profile and industry — sales-optimized every time",
    "feature2Title": "Digital Human & Voice Cloning",
    "feature2Desc": "Professional avatar library plus personal voice cloning — build a unique, consistent brand identity",
    "feature3Title": "Trending Topic Creation",
    "feature3Desc": "Real-time trending charts built in — one click turns any hot topic into your own marketing content"
  },
  "Differentiators": {
    "heading": "Why Choose ClipFlow?",
    "diff1Title": "No Filming Skills Required",
    "diff1Desc": "Fully AI-driven — anyone can produce professional marketing videos from day one",
    "diff2Title": "90% Lower Cost",
    "diff2Desc": "Compared to agency outsourcing, each video costs under $1.50 to produce",
    "diff3Title": "10x Publishing Speed",
    "diff3Desc": "End-to-end automation means 1 hour replaces what a traditional team needs a whole week to produce",
    "diff4Title": "Consistent Brand Identity",
    "diff4Desc": "Every script is driven by your IP profile — your brand voice stays consistent across all content",
    "diff5Title": "Multi-Platform Ready",
    "diff5Desc": "Supports TikTok, YouTube Shorts, Instagram Reels and more — publish everywhere with one click",
    "diff6Title": "Real-Time Trend Tracking",
    "diff6Desc": "Built-in trending charts keep you on top of what's viral so your content is always in the moment"
  },
  "SocialProof": {
    "heading": "Trusted by Thousands of Small Business Owners",
    "stat1Value": "10,000+",
    "stat1Label": "Videos Generated",
    "stat2Value": "3,000+",
    "stat2Label": "Business Owners",
    "stat3Value": "60s",
    "stat3Label": "Avg. Generation Time"
  },
  "CTA": {
    "heading": "Start Building Your Personal Brand Today",
    "subtext": "Free to join — generate 2 videos per day, no credit card required",
    "button": "Start Free"
  },
  "Footer": {
    "copyright": "ClipFlow 2026",
    "tagline": "AI-powered marketing video pipeline"
  }
}
```
  </action>
  <acceptance_criteria>
    - Both files have zero empty string values
    - Both files have identical top-level key structure
    - Hero.title zh is "60 秒，一条专业营销短视频"
    - Hero.title en is "A Professional Marketing Video in 60 Seconds"
  </acceptance_criteria>
  <done>Both translation files filled with real bilingual copy — zero empty strings.</done>
</task>

<task type="auto">
  <name>Task 2: Build Hero section</name>
  <files>
    apps/web/src/components/marketing/hero-section.tsx
  </files>
  <action>
Design system: Primary #6366F1, CTA #22C55E, Background #EEF2FF, Text #312E81. Flat design. shadcn/ui Button only. Lucide icons only.

Create `apps/web/src/components/marketing/hero-section.tsx` — Server Component:

```tsx
import { useTranslations } from "next-intl"
import { Button } from "@/components/ui/button"
import { ArrowRight, Play } from "lucide-react"

export function HeroSection() {
  const t = useTranslations("Hero")

  return (
    <section className="bg-[#EEF2FF] py-20 sm:py-28 lg:py-36 px-4 sm:px-6 lg:px-8">
      <div className="mx-auto max-w-4xl text-center">
        <h1 className="text-4xl sm:text-5xl lg:text-6xl font-bold text-[#312E81] leading-tight tracking-tight">
          {t("title")}
        </h1>
        <p className="mt-6 text-lg sm:text-xl text-[#312E81]/70 max-w-2xl mx-auto leading-relaxed">
          {t("subtitle")}
        </p>
        <div className="mt-10 flex flex-col sm:flex-row gap-4 justify-center items-center">
          <Button
            size="lg"
            className="cursor-pointer bg-[#22C55E] hover:bg-[#16A34A] text-white text-base px-8 py-6 h-auto font-semibold transition-colors duration-200 w-full sm:w-auto"
          >
            {t("cta")}
            <ArrowRight className="ml-2 h-5 w-5" />
          </Button>
          <Button
            variant="outline"
            size="lg"
            className="cursor-pointer border-[#6366F1] text-[#6366F1] hover:bg-[#6366F1]/10 text-base px-8 py-6 h-auto font-semibold transition-colors duration-200 w-full sm:w-auto"
          >
            <Play className="mr-2 h-5 w-5" />
            {t("ctaSecondary")}
          </Button>
        </div>
      </div>
    </section>
  )
}
```
  </action>
  <acceptance_criteria>
    - No "use client"
    - Uses useTranslations("Hero")
    - Has both CTA buttons with ArrowRight and Play icons
    - bg-[#EEF2FF] background, text-[#312E81] text
    - Green primary CTA, indigo outline secondary CTA
  </acceptance_criteria>
  <done>HeroSection built with real copy from translations, two CTAs, flat design.</done>
</task>

<task type="auto">
  <name>Task 3: Build Pain Points section</name>
  <files>
    apps/web/src/components/marketing/pain-points-section.tsx
  </files>
  <action>
Create `apps/web/src/components/marketing/pain-points-section.tsx` — Server Component:

```tsx
import { useTranslations } from "next-intl"
import { DollarSign, Clock, PenLine, TrendingDown } from "lucide-react"

const painIcons = [DollarSign, Clock, PenLine, TrendingDown]

export function PainPointsSection() {
  const t = useTranslations("PainPoints")

  const pains = [
    { titleKey: "pain1Title" as const, descKey: "pain1Desc" as const, Icon: painIcons[0] },
    { titleKey: "pain2Title" as const, descKey: "pain2Desc" as const, Icon: painIcons[1] },
    { titleKey: "pain3Title" as const, descKey: "pain3Desc" as const, Icon: painIcons[2] },
    { titleKey: "pain4Title" as const, descKey: "pain4Desc" as const, Icon: painIcons[3] },
  ]

  return (
    <section className="py-16 sm:py-20 px-4 sm:px-6 lg:px-8 bg-white">
      <div className="mx-auto max-w-7xl">
        <h2 className="text-3xl sm:text-4xl font-bold text-[#312E81] text-center mb-12">
          {t("heading")}
        </h2>
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
          {pains.map(({ titleKey, descKey, Icon }) => (
            <div
              key={titleKey}
              className="flex flex-col items-start gap-4 rounded-xl border border-[#6366F1]/20 bg-[#EEF2FF]/60 p-6"
            >
              <div className="flex h-12 w-12 items-center justify-center rounded-lg bg-[#6366F1]/10">
                <Icon className="h-6 w-6 text-[#6366F1]" />
              </div>
              <div>
                <h3 className="text-lg font-semibold text-[#312E81] mb-2">{t(titleKey)}</h3>
                <p className="text-sm text-[#312E81]/70 leading-relaxed">{t(descKey)}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>
  )
}
```
  </action>
  <acceptance_criteria>
    - No "use client"
    - Uses useTranslations("PainPoints")
    - 4 pain points in a responsive grid
    - Lucide icons — no emojis
  </acceptance_criteria>
  <done>PainPointsSection with 4 pain points, Lucide icons, indigo accent colors.</done>
</task>

<task type="auto">
  <name>Task 4: Build How It Works section</name>
  <files>
    apps/web/src/components/marketing/how-it-works-section.tsx
  </files>
  <action>
Create `apps/web/src/components/marketing/how-it-works-section.tsx` — Server Component:

```tsx
import { useTranslations } from "next-intl"
import { FileText, UserSquare, Zap } from "lucide-react"

export function HowItWorksSection() {
  const t = useTranslations("HowItWorks")

  const steps = [
    { num: 1, titleKey: "step1Title" as const, descKey: "step1Desc" as const, Icon: FileText },
    { num: 2, titleKey: "step2Title" as const, descKey: "step2Desc" as const, Icon: UserSquare },
    { num: 3, titleKey: "step3Title" as const, descKey: "step3Desc" as const, Icon: Zap },
  ]

  return (
    <section className="py-16 sm:py-20 px-4 sm:px-6 lg:px-8 bg-[#EEF2FF]/50">
      <div className="mx-auto max-w-7xl">
        <h2 className="text-3xl sm:text-4xl font-bold text-[#312E81] text-center mb-12">
          {t("heading")}
        </h2>
        <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
          {steps.map(({ num, titleKey, descKey, Icon }) => (
            <div key={num} className="flex flex-col items-center text-center gap-4">
              <div className="relative flex h-16 w-16 items-center justify-center rounded-full bg-[#6366F1] text-white">
                <Icon className="h-7 w-7" />
                <span className="absolute -top-2 -right-2 flex h-6 w-6 items-center justify-center rounded-full bg-[#22C55E] text-xs font-bold text-white">
                  {num}
                </span>
              </div>
              <div>
                <h3 className="text-xl font-semibold text-[#312E81] mb-2">{t(titleKey)}</h3>
                <p className="text-[#312E81]/70 leading-relaxed">{t(descKey)}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>
  )
}
```
  </action>
  <acceptance_criteria>
    - No "use client"
    - Uses useTranslations("HowItWorks")
    - 3 steps with numbered circles
  </acceptance_criteria>
  <done>HowItWorksSection with 3 numbered steps, icons, and translated copy.</done>
</task>

<task type="auto">
  <name>Task 5: Build Features section</name>
  <files>
    apps/web/src/components/marketing/features-section.tsx
  </files>
  <action>
Create `apps/web/src/components/marketing/features-section.tsx` — Server Component:

```tsx
import { useTranslations } from "next-intl"
import { Sparkles, Video, Flame } from "lucide-react"

export function FeaturesSection() {
  const t = useTranslations("Features")

  const features = [
    { titleKey: "feature1Title" as const, descKey: "feature1Desc" as const, Icon: Sparkles },
    { titleKey: "feature2Title" as const, descKey: "feature2Desc" as const, Icon: Video },
    { titleKey: "feature3Title" as const, descKey: "feature3Desc" as const, Icon: Flame },
  ]

  return (
    <section className="py-16 sm:py-20 px-4 sm:px-6 lg:px-8 bg-white">
      <div className="mx-auto max-w-7xl">
        <h2 className="text-3xl sm:text-4xl font-bold text-[#312E81] text-center mb-12">
          {t("heading")}
        </h2>
        <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
          {features.map(({ titleKey, descKey, Icon }) => (
            <div
              key={titleKey}
              className="flex flex-col gap-4 rounded-xl border border-[#6366F1]/20 p-8 bg-[#EEF2FF]/40"
            >
              <div className="flex h-12 w-12 items-center justify-center rounded-lg bg-[#6366F1]">
                <Icon className="h-6 w-6 text-white" />
              </div>
              <h3 className="text-xl font-semibold text-[#312E81]">{t(titleKey)}</h3>
              <p className="text-[#312E81]/70 leading-relaxed">{t(descKey)}</p>
            </div>
          ))}
        </div>
      </div>
    </section>
  )
}
```
  </action>
  <acceptance_criteria>
    - No "use client"
    - Uses useTranslations("Features")
    - 3 feature cards with indigo icon backgrounds
  </acceptance_criteria>
  <done>FeaturesSection with 3 feature cards using indigo palette.</done>
</task>

<task type="auto">
  <name>Task 6: Build Differentiators section</name>
  <files>
    apps/web/src/components/marketing/differentiators-section.tsx
  </files>
  <action>
Create `apps/web/src/components/marketing/differentiators-section.tsx` — Server Component:

```tsx
import { useTranslations } from "next-intl"
import { CheckCircle } from "lucide-react"

export function DifferentiatorsSection() {
  const t = useTranslations("Differentiators")

  const diffs = [
    { titleKey: "diff1Title" as const, descKey: "diff1Desc" as const },
    { titleKey: "diff2Title" as const, descKey: "diff2Desc" as const },
    { titleKey: "diff3Title" as const, descKey: "diff3Desc" as const },
    { titleKey: "diff4Title" as const, descKey: "diff4Desc" as const },
    { titleKey: "diff5Title" as const, descKey: "diff5Desc" as const },
    { titleKey: "diff6Title" as const, descKey: "diff6Desc" as const },
  ]

  return (
    <section className="py-16 sm:py-20 px-4 sm:px-6 lg:px-8 bg-[#6366F1]">
      <div className="mx-auto max-w-7xl">
        <h2 className="text-3xl sm:text-4xl font-bold text-white text-center mb-12">
          {t("heading")}
        </h2>
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
          {diffs.map(({ titleKey, descKey }) => (
            <div key={titleKey} className="flex items-start gap-4 rounded-xl bg-white/10 p-6">
              <CheckCircle className="h-6 w-6 text-[#22C55E] shrink-0 mt-0.5" />
              <div>
                <h3 className="text-lg font-semibold text-white mb-1">{t(titleKey)}</h3>
                <p className="text-white/70 text-sm leading-relaxed">{t(descKey)}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>
  )
}
```
  </action>
  <acceptance_criteria>
    - No "use client"
    - Uses useTranslations("Differentiators")
    - 6 items on indigo background with green checkmarks
  </acceptance_criteria>
  <done>DifferentiatorsSection with 6 items on brand-indigo background.</done>
</task>

<task type="auto">
  <name>Task 7: Build Social Proof section</name>
  <files>
    apps/web/src/components/marketing/social-proof-section.tsx
  </files>
  <action>
Create `apps/web/src/components/marketing/social-proof-section.tsx` — Server Component:

```tsx
import { useTranslations } from "next-intl"

export function SocialProofSection() {
  const t = useTranslations("SocialProof")

  const stats = [
    { valueKey: "stat1Value" as const, labelKey: "stat1Label" as const },
    { valueKey: "stat2Value" as const, labelKey: "stat2Label" as const },
    { valueKey: "stat3Value" as const, labelKey: "stat3Label" as const },
  ]

  return (
    <section className="py-16 sm:py-20 px-4 sm:px-6 lg:px-8 bg-white">
      <div className="mx-auto max-w-7xl">
        <h2 className="text-3xl sm:text-4xl font-bold text-[#312E81] text-center mb-12">
          {t("heading")}
        </h2>
        <div className="grid grid-cols-1 sm:grid-cols-3 gap-8">
          {stats.map(({ valueKey, labelKey }) => (
            <div key={valueKey} className="flex flex-col items-center text-center gap-2 py-8 rounded-xl bg-[#EEF2FF] border border-[#6366F1]/20">
              <span className="text-5xl font-bold text-[#6366F1]">{t(valueKey)}</span>
              <span className="text-[#312E81]/70 font-medium">{t(labelKey)}</span>
            </div>
          ))}
        </div>
      </div>
    </section>
  )
}
```
  </action>
  <acceptance_criteria>
    - No "use client"
    - Uses useTranslations("SocialProof")
    - 3 stat cards with large indigo numbers
  </acceptance_criteria>
  <done>SocialProofSection with 3 stat cards.</done>
</task>

<task type="auto">
  <name>Task 8: Build CTA section</name>
  <files>
    apps/web/src/components/marketing/cta-section.tsx
  </files>
  <action>
Create `apps/web/src/components/marketing/cta-section.tsx` — Server Component:

```tsx
import { useTranslations } from "next-intl"
import { Button } from "@/components/ui/button"
import { ArrowRight } from "lucide-react"

export function CTASection() {
  const t = useTranslations("CTA")

  return (
    <section className="py-20 sm:py-28 px-4 sm:px-6 lg:px-8 bg-[#EEF2FF]">
      <div className="mx-auto max-w-3xl text-center">
        <h2 className="text-3xl sm:text-4xl lg:text-5xl font-bold text-[#312E81] mb-6">
          {t("heading")}
        </h2>
        <p className="text-lg text-[#312E81]/70 mb-10 leading-relaxed">
          {t("subtext")}
        </p>
        <Button
          size="lg"
          className="cursor-pointer bg-[#22C55E] hover:bg-[#16A34A] text-white text-base px-10 py-6 h-auto font-semibold transition-colors duration-200"
        >
          {t("button")}
          <ArrowRight className="ml-2 h-5 w-5" />
        </Button>
      </div>
    </section>
  )
}
```
  </action>
  <acceptance_criteria>
    - No "use client"
    - Uses useTranslations("CTA")
    - Green CTA button, #EEF2FF background
  </acceptance_criteria>
  <done>CTASection with heading, subtext, and green CTA button.</done>
</task>

<task type="auto">
  <name>Task 9: Assemble all sections in landing page</name>
  <files>
    apps/web/src/app/(marketing)/page.tsx
  </files>
  <action>
Update `apps/web/src/app/(marketing)/page.tsx` to import and render all 7 section components in order. Keep the existing generateMetadata() function unchanged.

```tsx
import { cookies } from "next/headers"
import type { Metadata } from "next"
import { HeroSection } from "@/components/marketing/hero-section"
import { PainPointsSection } from "@/components/marketing/pain-points-section"
import { HowItWorksSection } from "@/components/marketing/how-it-works-section"
import { FeaturesSection } from "@/components/marketing/features-section"
import { DifferentiatorsSection } from "@/components/marketing/differentiators-section"
import { SocialProofSection } from "@/components/marketing/social-proof-section"
import { CTASection } from "@/components/marketing/cta-section"

export async function generateMetadata(): Promise<Metadata> {
  const store = await cookies()
  const locale = store.get("locale")?.value ?? "zh"
  const isZh = locale === "zh"

  return {
    title: isZh
      ? "ClipFlow - AI 营销短视频流水线"
      : "ClipFlow - AI Video Marketing Pipeline",
    description: isZh
      ? "小企业主的 AI 视频创作助手——输入文案，自动生成专业营销短视频"
      : "AI-powered video creation for small businesses — from copy to professional marketing video",
    openGraph: {
      title: isZh ? "ClipFlow - AI 营销短视频流水线" : "ClipFlow - AI Video Marketing Pipeline",
      description: isZh
        ? "小企业主的 AI 视频创作助手"
        : "AI-powered video creation for small businesses",
      images: [{ url: "/og-image.png", width: 1200, height: 630 }],
      locale: isZh ? "zh_CN" : "en_US",
      type: "website",
    },
    alternates: {
      canonical: "https://clipflow.ai/",
      languages: {
        "zh-CN": "https://clipflow.ai/",
        "en": "https://clipflow.ai/",
        "x-default": "https://clipflow.ai/",
      },
    },
  }
}

export default function MarketingPage() {
  return (
    <>
      <HeroSection />
      <PainPointsSection />
      <HowItWorksSection />
      <FeaturesSection />
      <DifferentiatorsSection />
      <SocialProofSection />
      <CTASection />
    </>
  )
}
```
  </action>
  <acceptance_criteria>
    - page.tsx imports all 7 section components
    - generateMetadata() preserved
    - No "use client" on the page
    - No hardcoded text in the page component
  </acceptance_criteria>
  <done>Landing page assembles all 7 sections in order.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes — all sections render without TypeScript errors
2. `grep -rn "use client" apps/web/src/components/marketing/` — only language-switcher.tsx should have "use client"
3. `grep -rn "\"60\|\"A Professional\|\"三步\|\"Three Steps" apps/web/src/components/marketing/` — should return zero (all copy in translation files)
4. `node -e "const zh=require('./apps/web/messages/zh.json'); const vals=Object.values(zh).flatMap(Object.values); console.log(vals.filter(v=>v==='').length + ' empty values')"` — should return 0
</verification>

<success_criteria>
- All 7 sections assembled: Hero, PainPoints, HowItWorks, Features, Differentiators, SocialProof, CTA
- Real bilingual copy in both translation files — zero empty strings
- All components are Server Components (except language-switcher)
- No hardcoded strings in any component
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-content-sections/05-01-SUMMARY.md`
</output>
