# Claude 工作指引

> 本文件作为 Claude 工作指引的入口，将 Agent 角色与产品内核、部署运维分别委托给 `AGENTS.md` 和 `PROJECT.md`。

## Agent 角色与产品内核

见本目录 `AGENTS.md`（硅基战略局 / 苏格拉底式追问 / 产品内核 / 零 Mock 铁律）。

## 部署运维

见本目录 `PROJECT.md`。

## 文档真源

- 规则入口：`AGENTS.md`
- 部署与运行环境：`PROJECT.md`
- AIM 运行时内核与统一执行链：`docs/architecture/adr-001-aim-harness-execution-kernel.md`

以下目录默认不作为当前规则真源，除非已被上面这些文件或代码再次确认：

- `docs/plans/`：规划与待办
- `docs/reports/`：一次性报告
- `docs/superpowers/`：专项设计过程稿
- 其他未被入口文件引用的 `docs/*.md` / `docs/*.txt`：优先视为背景资料或历史材料
