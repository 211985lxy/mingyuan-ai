/**
 * Chat / Generate 共享的意图边界与轻改硬规则。
 * 两端必须复用同一文案，避免「优化开头」在 chat 只改局部、在 generate 却全文重写。
 */

export const AIM_NORTH_STAR_GOAL =
  "基于客户项目档案（IP Wiki / 知识库 / TaskSpec）生成可拍摄、可发布、可承接转化的针对性文案；严格服从本轮用户意图（新建 / 重写 / 轻改）；信息不足时标注「未提供/待补充」，禁止编造第一人称案例。"

/** 局部修改部位词典（与 resolveAimRuntimeTask 对齐） */
export const LOCAL_EDIT_PART_WORDS = [
  "开头", "前3秒", "前三秒", "第一句话", "第一句", "钩子", "起手", "开场",
  "标题", "结尾", "收尾", "CTA", "行动引导",
] as const

export const LIGHT_EDIT_OUTPUT_BOUNDARY = [
  "轻改输出边界：只改用户点名的部分，默认保留主题、正文主体和有效表达，禁止擅自整篇重写。",
  "如果用户只要求优化开头/前三秒/第一句话/钩子，输出只能是开头候选或开头替换稿，禁止返回整篇文案。",
  "如果要求标题只给标题；如果要求结尾只给结尾；如果要求 CTA 只给 CTA。",
  "如果用户要求优化/润色「这段/这篇/这段话」且已粘贴原文：只在原文基础上润色，保持相近篇幅与信息点；禁止扩写成全新长口播、长文或另起一篇。",
  "可以给开头、结构、结尾等简短可选建议，但不要把建议直接写进替换稿，也不要主动扩写客户背景或知识库素材。",
].join("\n")

export const LIGHT_EDIT_USER_INSTRUCTION =
  "请只根据用户原文、选区和修改要求做局部优化；替换稿只改用户点名的内容，不要顺手改未点名的开头、工具名、结尾或结构；如果用户只要求优化开头/前三秒/第一句话/钩子，只输出 3-5 个开头候选或一个开头替换稿，禁止输出整篇文案；可以给开头、结构、结尾等简短可选建议，但不要主动结合企业知识库扩写。"

/** Chat 会话优先级与纠偏原则（原 content_producer chat 原则 16–20） */
export const AIM_SESSION_PRIORITY_RULES = [
  "会话优先级固定为：当前轮用户修改要求 > 用户刚确认或刚点名的那版成稿 > 最近相关上下文 > 更早背景素材与知识库。用户说「这篇」「这版」「上面那个」时，默认指当前对话里最近相关的成稿或候选，不要回跳到更早那版。",
  "如果用户在纠偏、表达不满，或明确说「不要换」「别重写」「我不是这个意思」，先按他的纠正修改，不要继续执行上一轮默认流程。",
  "如果用户说「改开头」「改第一句」「改前3秒」「结合这篇稿子去改上面文案」，就按局部修改处理：只改用户点名的部分，默认保留当前稿子的主题、正文主体和有效表达，不要擅自整篇重写或切回最早素材。",
  "如果用户要求「结合他的资料/人设/IP故事/来时路自然融入」，要把资料化进正文推进、案例、判断和身份表达里，不要单独拼一段履历总结，也不要生硬堆标签。",
  "如果用户表达了「别越改越短」「保持原稿长度/体量」「不要压缩」这类意图，就默认保留当前稿子的主体信息密度和篇幅；除非用户明确要求精简，否则不要主动缩成短版。",
].join("\n")

/** 内部 CoT 闸门：只在内部完成，禁止输出思维过程 */
export const AIM_INTERNAL_INTENT_GATE =
  "开写前先在内部用一句话复述：本轮 runtimeTask（新建/重写/轻改）+ 用户真实意图 + 必须保留/禁止改动的范围；确认后再动笔。正式成稿禁止输出该复述或思维过程（METHOD_NOTE 教学拆解除外）。"

export const RUNTIME_TASK_LABELS: Record<string, string> = {
  light_edit: "轻改（只改点名局部）",
  rewrite_copy: "重写/对标改写",
  new_copy: "新建成稿",
  positioning_topic: "定位/选题策划",
  quality_review: "发布前质检",
}
