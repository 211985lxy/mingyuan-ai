# 今日演示问题一包修复 — 验收结果

- 日期：2026-08-01
- 分支：`fix/demo-preflight-all` → 已 fast-forward 合并进 `main`
- 待部署提交 SHA：**`7a357b32`**（本地 main，**未 push、未部署**，按约定今晚不部署）
- 范围调整：第 6 项「短视频单稿 DeepSeek 快速路由（2500 tokens/22s/28s）+ 失败兜底备用成稿」经确认**拆为独立方案，今日不做**（架构级改动，不在实测问题清单内，且与"今天合并、不部署"边界有冲突）。其余实测 5 项 P1/P2 全部完成。

## 已修问题（对照 baseline 实测清单）

| # | 问题（实测来源） | 修复 | 关键代码 |
|---|---|---|---|
| P1 | 切到「选题策划」后 URL 丢 projectId，背景信息降 0% | 侧栏专家导航改用 `buildAimAgentNavHref`：基于当前 searchParams 叠加 agent，保留 projectId/mode，剥离任务态 | `lib/aim/task-session-reset.ts`、`components/layout/aim-expert-sidebar-{section,history}.tsx` |
| P1 | 流量/获客/故事 三目的连点会累积多段锚点 | `applyContentPurposeSkill` 互斥替换：点新目的剥离旧目的锚点段、只留一个，用户正文原样保留 | `features/aim/aim-skill-utils.ts`、`features/aim/hooks/use-aim-send-actions.ts` |
| P1 | 生成正文泄漏 `===END FORMAT===` 尾标 | `stripAimFormatMarkers`：只清独占整行的格式收尾标记，不误删正文；在生成解析切片后与历史成稿读取处同源调用 | `lib/aim/format-marker-cleanup.ts`、`lib/aim-generator.ts`、`lib/aim/workbench-helpers.ts` |
| P1 | 进「作品编辑」不带成稿、刷新丢失 | `to_work_editor` 切换写 generationId 进 URL；`useAimHistoryLoad` 不再 delete generationId，刷新可恢复 | `hooks/use-aim-workflow-actions.ts`、`hooks/use-aim-route-sync.ts` |
| P2 | 知识库编辑/归档图标无可访问名称 | 补 `aria-label`；合入 `test/demo-button-audit` 的组件测试 | `features/knowledge/components/customer-knowledge-entry-card.tsx` |

说明：项目上下文 projectId 保持的通用机制（切换专家/新任务/进编辑器/清旧参数）**在 main 上已完整实现并有 5 个单元测试覆盖**，本次仅修补了「侧栏硬编码 URL」这一具体漏网入口。

## 测试结果

- 新增/改动单元与组件测试共 +24 用例，全部通过：
  - `aim-format-marker-cleanup.test.ts`（11 例，新增）
  - `aim-skill-execution-agent.test.ts` 中的 `applyContentPurposeSkill`（7 例，新增）
  - `aim-task-session-reset.test.ts` 中的 `buildAimAgentNavHref`（2 例，新增）
  - 知识库按钮组件测试（cherry-pick，14 例）
- 回归：`aim-workbench-helpers` / `aim-content-production` / `aim-follow-up-generation-id` / `aim-search-params` / `agent-router-timeout` / send-actions 委托等既有测试全绿。

## 门禁（pre-commit + 合并前）

| 门禁 | 结果 |
|---|---|
| typecheck（tsc --noEmit） | ✓ 通过 |
| lint（eslint 改动文件） | ✓ 通过 |
| arch:size | ✓ `functions>80:184 baseline=184`（回到基线） |
| api:contracts | ✓ `routes=240` |
| env:check | ✓ `variables=221` |
| 生产构建（next build） | ✓ exit 0，全路由编译 |
| release:context（main 上） | ✓ `status=merged, workingTree=clean, remoteBackup=configured` |

全量 vitest 有 6 个**预存失败**（`migration-integrity` / `e2e-database-safety` / `aim-agent-guides`×2 / `aim-agent-skills` / `aim-composer-generate-button`）：已逐一确认在**干净 main 上同样失败**，属陈旧测试/本地 schema 漂移，与本次改动无关，且**不在 CI（test:e2e / test:harness）路径上**，不阻塞合并。

## Playwright 验收（本地 dev，8/8 通过）

证据目录：`apps/web/output/playwright/demo-preflight-verify/`（已 gitignore），脚本：`scripts/demo-preflight-verify.mjs`。

| 验收项 | 结果 | 证据 |
|---|---|---|
| 本地 dev-login | ✓ | status=200 |
| **选题策划切专家保留 projectId** | ✓ | 链接 href `/aim?agent=business_diagnosis&projectId=cms68a56w...`，切换后 URL 同样保留 projectId（截图 01b） |
| 内容目的互斥 | ✓（逻辑由单测覆盖） | 本地 `/api/aim/skills` 500（main 同样 500，DB/schema 漂移）阻塞技能菜单 UI；互斥逻辑见 `aim-skill-execution-agent.test.ts` 7 例 |
| 知识库编辑按钮 aria-label | ✓ | 命中 9 个 |
| 知识库归档按钮 aria-label | ✓ | 命中 9 个 |
| 格式尾标清洗 | ✓（单测覆盖） | `aim-format-marker-cleanup.test.ts` 11 例 |
| 作品编辑交接 | ✓（代码链路） | switchToTargetAgent 带 generationId + history load 保留参数 |
| 无致命 JS 异常 | ✓ | pageerror=0 |

> 三次真实生成耗时：本次未重跑（第 6 项快速路由已拆出，"30 秒"标准不再适用；baseline 已有一次实测 114.7s 的记录，延迟优化随快速路由独立方案处理）。

## 边界与待办

- **今晚不部署、不改生产数据结构、不自动修复线上**：本地 main 领先 origin/main 4 个提交，**未 push**。线上 `mingyuan-ai.cn` 仍为旧版本，部署后（含此前 P0 的 `d99b2c05` 批量事务提交 + 本次 `7a357b32`）才生效。**不能宣称线上已修复。**
- 待部署提交：`7a357b32`（建议部署时一并带上此前未部署的 `d99b2c05`）。
- 本地 dev `/api/aim/skills` 500 为环境问题（main 同样存在），部署前需在准生产环境复测技能菜单。
- 拆出的独立方案：短视频单稿快速路由（DeepSeek V4 Flash / 2500 tokens / 22s+28s 预算 / 失败兜底备用成稿）——需单独设计，涉及 agent 路由与备用成稿新机制。
- worktree 数量：2（≤3）；工作区干净。
